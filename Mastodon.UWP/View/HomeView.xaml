<UserControl
    x:Class="Mastodon.UWP.View.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mastodon.UWP.View"
    xmlns:control="using:Mastodon.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:api="using:Mastodon.API.Models"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Loaded="UserControl_Loaded">

    <ListView VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ItemsSource="{x:Bind StatusList}" SelectionMode="None">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="api:StatusModel">
                <control:StatusControl HorizontalAlignment="Stretch" Margin="0 5 0 5"/>
            </DataTemplate>
        </ListView.ItemTemplate>
        <ListView.ItemContainerStyle>
            <Style TargetType="ListViewItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            </Style>
        </ListView.ItemContainerStyle>
    </ListView>
</UserControl>
